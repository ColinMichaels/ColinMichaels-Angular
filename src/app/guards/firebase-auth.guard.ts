/**
 * @deprecated Import AuthGuard from `auth.guard` for new route definitions.
 * This alias preserves existing imports while keeping one guard implementation.
 */
export {AuthGuard as FirebaseAuthGuard} from './auth.guard';
