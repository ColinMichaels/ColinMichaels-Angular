{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null && (auth.token.admin === true || auth.token.cmsAdmin === true || auth.token.roles.admin === true)",
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "logs": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.cmsAdmin === true || auth.token.roles.admin === true)",
      ".write": "auth != null && (auth.token.admin === true || auth.token.cmsAdmin === true || auth.token.roles.admin === true)"
    },
    "public": {
      ".read": true,
      ".write": "auth != null && (auth.token.admin === true || auth.token.cmsAdmin === true || auth.token.roles.admin === true)"
    },
    "cms": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.cmsAdmin === true || auth.token.roles.admin === true)",
      ".write": "auth != null && (auth.token.admin === true || auth.token.cmsAdmin === true || auth.token.roles.admin === true)"
    }
  }
}
