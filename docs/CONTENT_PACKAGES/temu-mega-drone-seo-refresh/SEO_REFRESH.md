# Temu Full-Size Drone SEO Refresh

Status: **local editorial package only**. The production article has not been imported, updated, deployed, or republished by this work.

Target URL: <https://colinmichaels.com/blog/they-bought-a-full-size-temu-mega-drone>

Audit date: August 14, 2026

## Production Preflight Update — August 15

The anonymous production route still resolves to the expected slug and self-canonical and preserves the original publication time and media objects. Its public article remains the older version: the search title is **They Bought a Full-Size Temu Mega Drone—and Flew It**, the article H1 is **They Bought a Full-Size Temu Mega Drone. Of Course They Flew It.**, the visible article reports 1,426 words and seven minutes, and it does not yet contain the new evidence disclosure, 2026 reality check, eCFR source, topic/buyer-guide continuations, or two inline illustrations.

At 1280 × 720 and 390 × 844, the anonymous hydrated route remained self-canonical, scrollable, horizontally contained, and free of dialogs after the settle window. The mobile Contents control expanded successfully. A separate read-only public Firestore request then verified the exact document ID, legacy missing-revision state, effective revision `0`, dates, featured state, media, taxonomy, all 61 production blocks, and a complete SHA-256 document fingerprint. The staged package retains every original block ID, intentionally edits four, adds ten, and removes none.

The record-identity blocker is resolved, but the package is still **not safe to import** until Colin approves the editorial and featured-state changes and the exact record passes authenticated Production Preview. The machine-readable baseline, local validator, and live drift verifier fail closed on any production change:

- [`PRODUCTION_BASELINE_2026-08-15.json`](./PRODUCTION_BASELINE_2026-08-15.json)
- `/scripts/validate-temu-production-preflight.mjs`
- `/scripts/verify-temu-production-live.mjs`

## Why This Page Is First

The current Search Console baseline for July 17 through August 13 shows `105` impressions, `1` click, `0.95%` click-through rate, and average position `9.9`. The page is already close enough to page one to earn attention, but its search packaging and article depth are not converting that visibility well.

A live GA4 recheck on August 15 strengthened that decision. For July 18 through August 14, the article led the visible page table with `159` views, `142` active users, `544` events, and a `56.6%` bounce rate. The homepage recorded `138` views and `57` active users in the same report. Page views still do not prove that every visit was a completed human read, but the raw-count lead is strong enough to make this the evidence-backed homepage feature candidate while the refresh is evaluated.

This refresh preserves the verified working URL, canonical, stable document ID, original creation/publication dates, every original block ID, media objects, taxonomy, and primary Goonzquad video. Immediately before import, `npm run verify:temu-production` must reconfirm the fingerprint and effective revision `0`. The package improves the promise, disclosure, evidence boundary, internal discovery, imagery, and crawler rendering without turning the story into a different article.

## Page Scorecard Before Refresh

These are transparent editorial/technical review scores, not Google scores or ranking guarantees.

| Area | Score | Main evidence |
| --- | ---: | --- |
| Overall | 81/100 | Strong premise, first-page proximity, useful original voice; weak click conversion and incomplete evidence framing |
| On-page SEO | 79/100 | Valid canonical and one H1; main query not led in the title, duplicate-title H2, and weak click payoff |
| Content quality | 76/100 | Concrete story and original commentary; no explicit research disclosure, thin legal/evidence boundary, and editorial body below the 1,500-word feature gate |
| Technical SEO | 92/100 | Indexable, canonical, semantic crawler fallback; fallback hero lacked intrinsic dimensions and could shift layout |
| Structured data | 90/100 | Valid `BlogPosting` identity, author, image, and dates; no meaningful modified-date freshness signal |
| Images | 72/100 | Relevant 1200 × 630 hero; generic/repetitive alt text, no intrinsic fallback dimensions, and no in-body images |

## Priority Issues

### High

- Search demand is visible, but `0.95%` CTR at average position `9.9` is the clearest current growth leak.
- The former title, **They Bought a Full-Size Temu Mega Drone—and Flew It**, does not lead with the observed query language and leaves the reader payoff implicit.
- The former description summarizes the event but does not promise the damaged-parts, assembly, flight, and proof-boundary answer that differentiates the story.
- The article did not explicitly say that Colin researched the public video rather than buying, assembling, inspecting, or flying the aircraft.
- The parsed live fallback was roughly 1,436 words even with surrounding metadata/taxonomy text included, leaving the editorial body below the local 1,500-word feature gate.
- The article had no contextual internal link into the broader drone collection and no in-body image blocks.

### Medium

- The first H2 repeated the page title instead of functioning as a descriptive section.
- Published and modified dates were identical, so the page exposed no meaningful update signal.
- The fallback hero was eager-loaded without intrinsic width/height or explicit fetch priority, creating avoidable layout-shift risk.
- The hero alt text repeated the headline rather than describing the visible reaction, aircraft, pallet, and display copy.

## Prepared Editorial Refresh

The exact-ID CMS package changes the visible and search title to:

> Temu Full-Size Drone: Goonzquad Bought It—and It Flew

The new description is:

> Goonzquad bought a full-size Temu drone, assembled it from crates, and flew it. See what arrived, what went wrong, and what the flight actually proved.

The package also:

- replaces the duplicate-title H2 with a `lead` typography block;
- adds a visible research and ownership disclosure without inferring a commercial relationship;
- adds typed researched-evidence metadata with the August 14 source-review date, relationship boundary, AI-assistance disclosure, synthetic-media disclosure, and a substantive update note;
- labels the original Goonzquad video as the primary evidence;
- expands the TLDR and separates visible proof from missing evidence;
- adds **The 2026 Reality Check: Flight Is Not Approval** with a cautious, source-adjacent Part 103 summary;
- adds a contextual buyer-verification guide with a printable worksheet beside that evidence boundary;
- changes `autonomous` to the more defensible `software-stabilized` description;
- links contextually to the Drones & FPV topic hub;
- adds two distinct, disclosed editorial illustrations with intrinsic dimensions;
- expands the reader poll from a binary reaction to four useful next-story choices;
- marks the stable post as `featured` so the existing automatic homepage policy can lead with the current traffic winner after the reviewed import;
- supplies a descriptive Open Graph alt and the existing image's verified `1200 × 630` dimensions;
- sets a new modified date while retaining the original publication date; and
- preserves all original block IDs and content except where the refresh intentionally adds or changes the items above.

The validated export contains `71` uniquely identified blocks, including `2` inline image blocks and `4` poll options.

The exact-ID import is also evidence-ready for the new article transparency surface. Its metadata treats Goonzquad's public video as the primary event evidence, keeps Part 103 as a source-bounded rules reference, states that Colin did not buy, inspect, assemble, or fly the aircraft, discloses AI assistance and both generated illustrations, and explains the substantive refresh. This does not classify or modify the live article until the reviewed exact record is explicitly approved and imported.

## Files

- CMS import: [`temu-mega-drone-seo-refresh-import.json`](./temu-mega-drone-seo-refresh-import.json)
- Captain Colin companion-video package: [`CAPTAIN_COLIN_VIDEO_PACKAGE.md`](./CAPTAIN_COLIN_VIDEO_PACKAGE.md)
- Damaged-parts illustration: `/src/assets/images/blog/drones/temu-mega-drone-evidence-check.webp`
- Part 103 planning illustration: `/src/assets/images/blog/drones/temu-mega-drone-part-103.webp`
- Captain Colin YouTube thumbnail: `/src/assets/images/blog/drones/temu-mega-drone-youtube-thumbnail.jpg`

The JSON uses the stable production document ID `post-041c74e0-31f7-460a-8e85-4be3d62d9622`, slug `they-bought-a-full-size-temu-mega-drone`, and publication time `2026-07-11T01:11:53.159Z`. Because its status is `published`, it is an update package—not a safe scratch import.

## Image Source and Prompt Ledger

Both images were generated with the built-in Codex image-generation tool, converted to `1500 × 1000` WebP derivatives, and captioned so readers cannot mistake them for photographs of the Goonzquad shipment or aircraft.

The separate `1280 × 720` YouTube thumbnail is also an AI-generated editorial illustration. Its visible **COMMENTARY** label and package rights note distinguish it from Goonzquad footage, the actual aircraft, and a Temu listing.

### Damaged-parts inspection

- Purpose: make the visible shipping-damage boundary concrete beside the damaged-propeller paragraph.
- Prompt: photorealistic editorial workshop inspection of a generic large single-seat multicopter kit; a carbon-fiber propeller with one chipped tip, inspection tape, torque wrench, flashlight, caliper, and blank checklist; open crates and generic blue airframe softly out of focus; analytical natural light; no people, crash, brands, readable text, registration, or claim of depicting Temu or Goonzquad evidence.
- Alt: `Inspection light, checklist, and tools beside a chipped generic multicopter propeller in a workshop`
- Disclosure: AI-generated editorial illustration; not documentary evidence.

### Part 103 operating reality

- Purpose: show that a personal multicopter still requires real operating and airspace planning rather than functioning as a city commuter.
- Prompt: photorealistic editorial scene with a generic blue single-seat multicopter parked at an open rural field beside a planning table, helmet, abstract tablet map, paper charts, pencil, and weather meter; distant small town and windsock; stopped propellers; no people, urban flight, crowds, logos, FAA seals, readable text, or claim that the chart is official.
- Alt: `Generic single-seat multicopter parked beside a flight-planning table at an open rural field`
- Disclosure: AI-generated editorial illustration; not an official chart or legal determination.

## Source Ledger

| Claim or evidence | Source | Use |
| --- | --- | --- |
| What arrived, how it was assembled, visible damage, translated controls, remote test, and low occupied flight | [Goonzquad original video](https://www.youtube.com/watch?v=bUkvDe0x47A) | Primary evidence for what the video visibly shows |
| Current U.S. ultralight definition and operating limits | [14 CFR Part 103](https://www.ecfr.gov/current/title-14/chapter-I/subchapter-F/part-103) | Primary legal source; article avoids making an aircraft-specific qualification determination |
| Existing page title, description, headings, image, links, and structured data | [Live article](https://colinmichaels.com/blog/they-bought-a-full-size-temu-mega-drone) | Live page audit captured August 14, 2026 |
| Impressions, clicks, CTR, and position | Local creator-growth baseline in `docs/SEO/CREATOR_GROWTH_OPERATING_SYSTEM.md` | First-party Search Console snapshot; time-bound and low-volume |

August 15 reverification confirmed that YouTube's official oEmbed endpoint still resolves `bUkvDe0x47A` as **We Bought Temu's Craziest Product!!!** by `goonzquad`, and the current eCFR Part 103 page displays regulatory text through August 13, 2026. The detailed visible-event review remains dated August 14; metadata resolution alone is not a substitute for rewatching the source before final production.

## Publication Gate

Do not import this file merely to test it.

1. Finish and validate the Angular and Functions release that supplies the Drones & FPV hub, crawler metadata, and intrinsic fallback image markup.
2. Run the complete local test/build/lint set and package the current Functions SEO renderer from the exact tested Angular output.
3. Immediately before import, export or read the current production post again. Confirm the document ID, slug, revision, title, `updatedAt`, media URLs, and block IDs have not changed since this package was prepared.
4. Review the full diff in Production Preview, including both image captions, mobile rendering, link behavior, poll, and video.
5. Obtain explicit editorial/publication approval. Bulk importing this published exact-ID record can update the live article after confirmation.
6. Verify the public HTML, hydrated browser view, Firestore record, canonical, Open Graph image, modified date, structured data, internal link, image dimensions, console, and responsive layout.
   Confirm that the automatic homepage hero resolves this post as the newest featured story, that the story remains an H2 under the stable creator-promise H1, and that the prior hero is still reachable through the recent-post sequence.
   Also verify that the buyer-verification guide and printable PDF both return HTTP 200 and that its `resource_page` download event reaches the production analytics property without exposing a full URL or personal data.
7. Request recrawling only after the live page is confirmed. Judge CTR after enough new impressions; do not call a low-volume percentage swing a win.

Do not link the unpublished passenger-drone follow-up from this live article until that follow-up has its own approved public URL.

## Rollback

Before import, retain a fresh production export. If the refresh must be rolled back, re-import that exact prior record to restore its title, description, blocks, image metadata, and modified date while preserving the stable URL and document identity. A Functions/Hosting rollback should use the previously verified release; do not delete the article, assets, analytics history, or topic route.

## Validation

The package must pass:

```text
node /Users/colin/.codex/skills/create-colinmichaels-blog-post/scripts/validate_blog_import.mjs docs/CONTENT_PACKAGES/temu-mega-drone-seo-refresh/temu-mega-drone-seo-refresh-import.json
```

Current result: `Validated 1 post(s) with 0 warning(s)`.

The repository-level evidence gate also validates every prepared article package:

```text
npm run test:content-packages
```

Current result: `Validated 4 evidence-ready content package(s)` with `16` deduplicated explicit external references across the four packages.

Repository validation for the complete creator-growth worktree also passed:

- `npm run build`
- `npm run lint`
- `npm run build:functions`
- `npm run prepare:functions-seo`
- `npm test -- --watch=false --progress=false --reporters=dots` — `917` specs passed
- `npm --prefix functions run test:phase7` — `16` tests passed, including all four exact content packages at the trusted write boundary
- `node --test functions/test/seo-image-markup.test.cjs` — `2` tests passed
- `git diff --check`

Local validation proves schema and asset readiness. It does not prove production import, deployment, indexing, CTR improvement, or audience growth.
